/**
 * DevNotes — Background Service Worker
 *
 * Runs in the background as long as the extension is installed.
 * Responsibilities (future):
 *   - Handle extension lifecycle events
 *   - Manage context menus
 *   - Coordinate messaging between popup, side panel, and content scripts
 *   - Handle alarms and scheduled tasks
 *   - Manage storage operations
 */

console.log('[DevNotes] Background service worker initialized')
